// Use array inside an object

const adventurer = {
    name: "EJ",
    hitPoints: 10,
    belongings: ["sword", "shield", "helmet"]
}

// access the values in the array

console.log(adventurer.belongings);